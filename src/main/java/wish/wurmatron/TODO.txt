// TODO

 - World Gen
    - Open Terran Generator Settings
    - Custom Ore Generation
    - Custom Trees

 - Blocks
    - Rock Type Blocks
        - Raw (Able to collect small amount of ore via machines or tool)
        - Smooth
        - Chiseled
        - Brick
        - Stairs
        - Grass
        - Dirt
    - Ores for Each Rock Type along with quality
    - Trees Logs?

 - Items
    - Gems (Diffrent types in each rock type)
    - Ore Items (Diffrent Qualities)
    - Throwing Rock (Starting Axe)

 - Farming
    - Ore Mining Destroys Farmland
    - Integrate with Tough as Nails Seasons

    // Place Holder
    //	private static Random rand = new Random ();
    //
    //	@SubscribeEvent
    //	public void onEvent (PopulateChunkEvent.Pre e) {
    //		Chunk theChunk = e.getWorld ().getChunkFromChunkCoords (e.getChunkX (),e.getChunkZ ());
    //		int meta = rand.nextInt (8);
    //		for (int x = 0; x < 16; ++x) {
    //			for (int z = 0; z < 16; ++z) {
    //				for (int y = 0; y < 200; ++y) {
    //					if (theChunk.getBlockState (x,y,z).getBlock () instanceof BlockStone) {
    //						theChunk.setBlockState (new BlockPos (x,y,z),theChunk.getBlockState (x,y,z).getBlock ().getDefaultState ().withProperty (BlockRockType.TYPE,meta));
    //					}
    //				}
    //			}
    //		}
    //		theChunk.markDirty ();
    //	}